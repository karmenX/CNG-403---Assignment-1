{
  "training": {
    "batch_size": 64,
    "epochs": 20,
    "learning_rate": 0.01,
    "momentum": 0.9,
    "l2_lambda": 0.0001,
    "seed": 42
  },
  "model": {
    "hidden_sizes": [256, 128],
    "activation": "relu"
  },
  "paths": {
    "checkpoint_dir": "checkpoints/",
    "log_dir": "logs/"
  }
}
